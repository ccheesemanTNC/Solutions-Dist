import { IHubRequestOptions } from "@esri/hub-common";
/**
 * Remove the domain entries for a site
 * @param {string} siteId Item Id of the site to remove the domain entries for
 * @param {IHubRequestOptions} hubRequestOptions
 * @private
 */
export declare function _removeSiteDomains(siteId: string, hubRequestOptions: IHubRequestOptions): Promise<any>;

import { IInitiativeModel, IHubRequestOptions } from "@esri/hub-common";
/**
 * Update the tags on the teams after the initiative is created
 * Specifically:
 * - add  `hubInitiativeFollowers|<initiaiveId> to the followers group
 * @param {object} initiativeModel Initiative Model
 * @param {object} teams hash of teams
 * @param {IHubRequestOptions} hubRequestOptions
 * @private
 */
export declare function _updateTeamTags(initiativeModel: IInitiativeModel, teams: any, hubRequestOptions: IHubRequestOptions): Promise<{
    success: boolean;
}>;

import { IHubRequestOptions } from "@esri/hub-common";
/**
 * Gets the name of the resource for the current draft.
 * NOTE: There _should_ only be one, but sometimes it gets messed up.
 * @param siteOrPageId
 * @param hubRequestOptions
 * @private
 */
export declare function _getDraftResourceNames(siteOrPageId: string, hubRequestOptions: IHubRequestOptions): Promise<string[]>;

import { IModel, IHubRequestOptions } from "@esri/hub-common";
/**
 * Given an item id, removes the current draft resource if exists
 * @param {*} siteOrPageId
 * @param {*} hubRequestOptions
 */
export declare function deleteDraft(siteOrPageModel: IModel, hubRequestOptions: IHubRequestOptions): Promise<{
    success: boolean;
}[]>;

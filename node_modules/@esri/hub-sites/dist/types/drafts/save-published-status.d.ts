import { IHubUserRequestOptions, IModel } from "@esri/hub-common";
import { IUpdateItemResponse } from "@esri/arcgis-rest-portal";
/**
 * Saves the published status of a site or page model
 * leaving everything else on the model alone.
 *
 * @param siteOrPageModel
 * @param requestOptions
 */
export declare function savePublishedStatus(siteOrPageModel: IModel, requestOptions: IHubUserRequestOptions): Promise<IUpdateItemResponse>;

import { IDraft, IHubRequestOptions } from "@esri/hub-common";
/**
 * Fetches the draft for a site or page if exists.
 * @param {*} siteOrPageId
 * @param {*} hubRequestOptions
 */
export declare function fetchDraft(siteOrPageId: string, hubRequestOptions: IHubRequestOptions): Promise<IDraft>;

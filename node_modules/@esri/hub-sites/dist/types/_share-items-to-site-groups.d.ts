import { IModel, IHubRequestOptions } from "@esri/hub-common";
import { ISharingResponse } from "@esri/arcgis-rest-portal";
/**
 * Share all the other models to the Site's content and collaboration groups, if
 * those groups were created for the site (depends on user's privs)
 * **DEPRECATED: Use shareItemsToSiteGroups() instead**
 * @param {object} siteModel Site Model
 * @param {Array} solutionModels Array of all models created by the Solution
 * @param {IHubRequestOptions} hubRequestOptions
 * @private
 */
export declare function _shareItemsToSiteGroups(siteModel: IModel, solutionModels: IModel[], hubRequestOptions: IHubRequestOptions): Promise<ISharingResponse[]>;
/**
 * Share all the other models to the Site's content and collaboration groups, if
 * those groups were created for the site (depends on user's privs)
 * @param {object} siteModel Site Model
 * @param {Array} solutionModels Array of all models created by the Solution
 * @param {IHubRequestOptions} hubRequestOptions
 * @exported
 */
export declare function shareItemsToSiteGroups(siteModel: IModel, solutionModels: IModel[], hubRequestOptions: IHubRequestOptions): Promise<ISharingResponse[]>;

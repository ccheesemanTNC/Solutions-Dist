import { IModelTemplate, IHubRequestOptions, IInitiativeModel } from "@esri/hub-common";
/**
 * Given a Site Template, locate the initiative template
 * then adlib it and create the initiative item
 * @param {object} siteTemplate Site Template
 * @param {object} settings adlib interpolation hash
 * @param {object} transforms adlib transforms hash
 * @param {IHubRequestOptions}} hubRequestOptions
 * @private
 */
export declare function _createSiteInitiative(siteTemplate: IModelTemplate, settings: any, transforms: any, hubRequestOptions: IHubRequestOptions): Promise<IInitiativeModel>;

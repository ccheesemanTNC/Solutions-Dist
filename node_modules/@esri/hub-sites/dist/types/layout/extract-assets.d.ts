import { ISettings } from "./types";
/**
 * Extract the fileSrc and cropSrc assets from settings.
 *
 * @param {ISettings} settings
 */
export declare function extractAssets(settings: ISettings): string[];

import { ICard, ITemplatizedCard } from "./types";
/**
 * Convert a card to a templatized version of itself
 * @param {ICard} card the card to templatize
 */
export declare function convertCard(card: ICard): ITemplatizedCard;

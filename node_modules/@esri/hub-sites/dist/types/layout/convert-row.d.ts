import { IRow, ITemplatizedRow } from "./types";
/**
 * Convert a row, collecting assets along the way...
 * @param {IRow} row the row to templatize
 */
export declare function convertRow(row: IRow): ITemplatizedRow;

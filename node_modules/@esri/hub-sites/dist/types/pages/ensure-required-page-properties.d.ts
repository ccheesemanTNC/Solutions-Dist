import { IModelTemplate, IModel } from "@esri/hub-common";
/**
 * Given a Page Model, ensure that it has all the requires properties set correctly
 * and return a new object
 * @param {Object} pageModel Page Model object
 * @param {Object} options {username, isPortal}
 */
export declare function ensureRequiredPageProperties(pageModel: IModelTemplate, options: {
    username: string;
    isPortal: boolean;
}): IModel;

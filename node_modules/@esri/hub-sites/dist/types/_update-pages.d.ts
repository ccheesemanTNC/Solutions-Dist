import { IModel, IHubRequestOptions, IModelTemplate } from "@esri/hub-common";
import { IUpdateItemResponse } from "@esri/arcgis-rest-portal";
/**
 * Locate any Page items that were created in the Solution, and link them back to the Site
 * @param {object} siteModel Site Model
 * @param {Array} solutionModels Array of all models created by the Solution
 * @param {IHubRequestOptions} hubRequestOptions
 * @private
 */
export declare function _updatePages(siteModel: IModel, solutionModels: IModelTemplate[], hubRequestOptions: IHubRequestOptions): Promise<IUpdateItemResponse[]>;

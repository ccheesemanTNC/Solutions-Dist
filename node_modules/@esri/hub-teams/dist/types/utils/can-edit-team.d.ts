import { IGroup, IUser } from "@esri/arcgis-rest-types";
/**
 * Checks if user has access to edit a team
 * @param {IGroup} group
 * @param {IUser} user
 * @returns {boolean}
 */
export declare function canEditTeam(group: IGroup, user: IUser): boolean;

import { HubProduct } from "../types";
interface ITeamPropMap {
    content: string;
    core: string;
    followers?: string;
}
/**
 * Return a map from the team names to the property names
 * @param {string} product Product Name (basic, premium, enterprise)
 */
export declare function getTeamPropertiesMapForProduct(product: HubProduct): ITeamPropMap;
export {};

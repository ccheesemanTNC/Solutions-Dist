import { IGroupTemplate } from "../types";
/**
 * Inject the translations into the Group object template
 * @param {object} template Json Template for the Group
 * @param {string} title Group Title
 * @param {object} translation Translation json
 * @private
 */
export declare function _translateTeamTemplate(template: IGroupTemplate, title: string, translation: Record<string, any>): IGroupTemplate;

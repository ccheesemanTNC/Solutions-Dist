"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeTeam = void 0;
const arcgis_rest_portal_1 = require("@esri/arcgis-rest-portal");
/**
 * removeTeam unprotects a group, then deletes it
 *
 * @export
 * @param {string} id Team Id
 * @param {UserSession} authentication authentication
 * @return {*}  {Promise<{groupId: string; success: boolean}>}
 */
async function removeTeam(id, authentication) {
    // unprotect the group
    const unprotectResult = await arcgis_rest_portal_1.unprotectGroup({ id, authentication });
    // If that succeeded...
    return unprotectResult.success
        // Remove it.
        ? arcgis_rest_portal_1.removeGroup({ id, authentication })
        // Otherwise return a fail state
        : { groupId: id, success: false };
}
exports.removeTeam = removeTeam;
//# sourceMappingURL=remove-team.js.map